services:
  redis:
    image: redis:7
    command: --port 6379
    expose:
      - 6379
    env_file:
      - .env-non-dev
    restart: on-failure
  bot:
    build: .
    command: ["/app/docker/bot.sh"]
    env_file:
      - .env-non-dev
    depends_on:
      - redis